/* QUESTION 2.35  */
/* Count returns the total number of occurence of an item in a given column */
SELECT COUNT(SKU) 
FROM cape_codd.inventory;

/* Sum returns the sum total value from the addition of all values in a given column */
SELECT SUM(SKU) 
FROM cape_codd.inventory;